import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 20,
  },
  title: {
    color: '#ff9f1c',
    fontSize: 32,
    fontWeight: 'bold',
    marginBottom: 10,
  },
});
