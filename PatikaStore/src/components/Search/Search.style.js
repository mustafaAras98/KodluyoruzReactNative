import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#e5e5e5',
    borderRadius: 10,
  },
  textInput: {
    fontSize: 16,
    paddingLeft: 10,
  },
});
