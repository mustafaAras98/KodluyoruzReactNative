import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  Container: {
    marginTop: 0,
    marginBottom: 10,
    marginHorizontal: 10,
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  PageText: {padding: 10, color: '#36251B', fontSize: 18, fontWeight: 'bold'},
});

export default styles;
